import type { OutlineMap } from "@type-shards/core";

const outlines = {
  Dashboard: { em: 1000, ascender: 800, descender: -200, groups: [] },
  Settings: { em: 1000, ascender: 800, descender: -200, groups: [] },
} satisfies OutlineMap;

export default outlines;
