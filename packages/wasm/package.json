{
  "name": "@yuragi/wasm",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./runtime": {
      "types": "./dist/runtime.d.ts",
      "import": "./dist/runtime.js"
    },
    "./yuragi_wasm_compiler.wasm": "./dist/yuragi_wasm_compiler.wasm"
  },
  "files": ["dist"],
  "scripts": {
    "build": "node scripts/build-wasm.mjs && tsup src/index.ts src/runtime.ts --format esm --dts --sourcemap --clean --target es2022 && cp wasm/yuragi_wasm_compiler.wasm dist/yuragi_wasm_compiler.wasm",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@yuragi/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
